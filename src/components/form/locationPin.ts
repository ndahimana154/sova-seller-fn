import { divIcon } from 'leaflet'

export const pinIcon = divIcon({
  className: '',
  html: '<span class="block size-5 rounded-full border-[3px] border-white bg-primary shadow-[0_2px_8px_rgb(0,0,0,.35)]"></span>',
  iconAnchor: [10, 10],
  iconSize: [20, 20],
})
